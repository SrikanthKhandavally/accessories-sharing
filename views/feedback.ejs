<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title> Content Sharing - Accessories  </title>
    <link rel="stylesheet" type="text/css" href="../assets/stylesheets/feedback-style.css">
  </head>
  <body>
    <% include header.ejs %>

    <div class="nav-1">
      <% include user-navigation.ejs %>
    </div>

    <div class="page-content">
      <% include site-navigation.ejs %>

      <div class="article">
        <ul class="breadcrumb">
          <li><a href="/catagories">H<i>ome</i></a></li>
          <li>></li>
          <li><a href="/catagories">C<i>atagories</i></a></li>
          <li>></li>
          <li><i><%= item.name %></i></li>
        </ul>
        <div class="image-container">
          <img alt="item image" class="item-image" src="<%= item.imageUrl %>" />
          <div class="image-side-description">
            <p><strong><%= item.name %></strong></p>
            <p><%= item.category %></p>
            <!-- <ul class="ratings-bar">
              <% for(var i=0; i < item.rating; i++) { %>
                 <li> <img alt="star" class="ratings-image" src="../assets/images/star-image.png" /> </li>
               <% } %>
            </ul> -->
            <!-- <br> -->
            <p><%= item.description %></p>
            <div class="feedback-actions">
              <div class = "feedback-rating">
                <form action="/profile/update-rating" method="post">
                  <p> Rating </p>
                  <select name="user-rating">
                    <%if (item.rating === 1) {%> <option value="1" selected>1 Star</option>  <%}
                    else {%> <option value="1">1 Star</option>  <%} %>
                    <%if (item.rating === 2) {%> <option value="2" selected>2 Star</option>  <%}
                    else {%> <option value="2">2 Star</option>  <%} %>
                    <%if (item.rating === 3) {%> <option value="3" selected>3 Star</option>  <%}
                    else {%> <option value="3">3 Star</option>  <%} %>
                    <%if (item.rating === 4) {%> <option value="4" selected>4 Star</option>  <%}
                    else {%> <option value="4">4 Star</option>  <%} %>
                    <%if (item.rating === 5) {%> <option value="5" selected>5 Star</option>  <%}
                    else {%> <option value="5">5 Star</option>  <%} %>
                  </select>
                  <input type="hidden" name="item-code" value="<%= item.code %>" />
                  <button class="button" type="submit" name="button">Confirm Rating</button>
              </form>
              </div>
              <br>
              <form action="/profile/update-tested" method="post">
                <input type="hidden" name="item-code" value="<%= item.code %>" />
                <%if (isTested === "true") {%> <input type="radio" name="tested-it" value="true" checked> Yes  <%}
                else {%> <input type="radio" name="tested-it" value="true"> Yes  <%} %>
                <%if (isTested === "false") {%> <input type="radio" name="tested-it" value="false" checked> No  <%}
                else {%> <input type="radio" name="tested-it" value="false"> No  <%} %>
                <button class="made-it-button" type= "submit" >
                  I tested it
                  <img alt="heart-image" class="button-image" src="../assets/images/like.png">
                </button>
              </form>
          </div>
        </div>
        </div>
    </div>
  </div>

    <% include footer.ejs %>

  </body>
</html>
